{{!-- Modal Business --}}

{{!-- <div class="modal" id="resultsModal">
	<div class="modal-background"></div>
	<div class="modal-content">
		<div class="modal-card">
			<section class="modal-card-body">
				<!-- Content ... -->
			</section>
		</div>
	</div>
	<button class="modal-close is-large" id="scrape-modal" aria-label="close"></button>
</div> --}}

<!-- Modal -->
<div class="modal fade" id="resultsModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="resultsModalLabel">Articles Scraped</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
      </div>
    </div>
  </div>
</div>





<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<a class="navbar-brand" href="/">Mongoose Scraper</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
	 aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item active">
				<a class="nav-link" href="/">Home
					<span class="sr-only">(current)</span>
				</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="/saved">Saved Articles</a>
			</li>
			<li>
				<button class="btn btn-outline-secondary my-2 my-sm-0" id='scrape' type="submit" data-toggle="modal" data-target="#resultsModal">Scrape New Articles</button>

			</li>
		</ul>


	</div>
</nav>

<div class="jumbotron">
  <h1 class="display-4">Mongo(ose) Scraper</h1>
  <p class="lead">All the New York Times Technology News Fit To Scrape.</p>

</div>

{{#each this}} {{#if this._id}}

<div class="container">
	{{> article-panel}} {{!--
	<div class="card">
		<div class="card-content" id="article-{{this.id}}">
			<p class="title">
				No Articles
			</p>
			<p class="subtitle">
				{{#if this.saved}}
				<p>It looks like you haven't saved any articles. </p>
				{{else}}
				<p>There aren't any article yet, Press Scrape New Articles button to generate stories </p>
				{{/if}}
			</p>
		</div>
	</div> --}}
</div>


{{/if}} {{/each}}