<div class="card">
    <div class="card-block">
        <h4 class="card-title" id="article-title" data-article-id="{{this.id}}">
            {{this.headline}}
        </h4>
        <h6 class="card-subtitle subtitle">
            {{this.summary}}
        </h6>
        <a href="{{this.url}}" class="btn btn-light card-link">View on NYTimes</a>
        {{> article-buttons}}

    </div>

</div>

<div class="collapse" id="commentCollapse-{{this._id}}">
    <div class="card card-body">
        {{#if this.comments}}
        <ul class="list-group">
            {{#each this.comments}}
            <li class="list-group-item">{{this.text}}
                <button type="button" class="close delete" aria-label="Close" delete-comment-id="{{this._id}}">
                    <span aria-hidden="true">&times;</span>
                </button>
            </li>

            {{/each}}
        </ul>

        {{/if}}
        <div class="form-group">
            <label for="comment-textarea">Comment</label>
            <textarea name="" id="comment-textarea-{{this._id}}" class="form-control"></textarea>
        </div>
        <button class="btn btn-light submit-comment" data-article-id="{{this._id}}">Submit Comment</button>
    </div>
</div>

{{!--
<div class="modal" id="commentsModal">
    <div class="modal-background"></div>
    <div class="modal-card">
        <header class="modal-card-head">
            <p class="modal-card-title">Article Comments</p>
            <button class="delete" aria-label="close" id="close-comments-modal"></button>
        </header>
        <section class="modal-card-body" id="article-title">
            {{#if this.comments}}
            <ul>
                {{#each this.comments}}
                <li>
                    {{ text }}
                    <a class="delete" id="delete-comment" data-id="{{this._id}}"></a>
                </li>
                {{/each}}
            </ul>
            {{else}}
            <p>Article has no comments</p>
            {{/if}}
            <div class="control">
                <textarea class="textarea" id="note-text-area" type="text" placeholder="Comment on Article"></textarea>
            </div>

        </section>

        <footer class="modal-card-foot">
            <button class="button is-success" id="submit-note">Submit Comment</button>
        </footer>
    </div>
</div> --}} {{!-- {{>article-comment}} --}}